# CSS mime type override

Loosely based on https://github.com/clemstoquart/webextension-json-mime-type.

A simple web-extension that rewrites css mime type that proxy at my workplace looses when using Firefox.